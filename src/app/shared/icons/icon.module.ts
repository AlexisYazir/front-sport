import { NgModule } from '@angular/core';
import { LucideDynamicIcon } from '@lucide/angular';

@NgModule({
  imports: [LucideDynamicIcon],
  exports: [LucideDynamicIcon],
})
export class IconModule {}
